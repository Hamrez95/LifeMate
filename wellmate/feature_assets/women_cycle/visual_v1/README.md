# Women Health visual assets v1

Implementation assets for #608, #610 and #611. The existing Period Calendar remains canonical. All daily-log fields are optional and observational/non-diagnostic. Partner badge is presentation-only and never grants access. Touch targets are >=48dp; icon viewBox is 48x48; base stroke 1.75dp. Selected state uses surface + keyline + check, never color alone.