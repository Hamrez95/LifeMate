/// Native-only offline API placeholder for platforms without `dart:io`.
///
/// The protected SQLite-backed LifeMate offline runtime is intentionally not
/// exposed on web. Web keeps the existing secure-storage-backed transitional
/// replay path until a separately reviewed browser storage design exists.
library;
